apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy-manager.fullname" . }}
  labels:
    {{- include "deploy-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy-manager.selectorLabels" . | nindent 4 }}
