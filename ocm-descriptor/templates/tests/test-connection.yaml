apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploy-manager.fullname" . }}-test-connection"
  labels:
    {{- include "deploy-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploy-manager.fullname" . }}:{{ .Values.service.port }}/deploy-manager/healthz']
  restartPolicy: Never
